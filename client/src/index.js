import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import ApolloProvider from './apollo-provider/index';

ReactDOM.render(
    <App />,
  document.getElementById('root')
);
